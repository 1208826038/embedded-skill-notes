# BMS 核心算法与 Simulink 建模实现（SOC / SOH / 均衡 / 代码生成）

> 本章面向两类读者：一是准备 BMS 嵌入式/算法岗位的你，二是想从"会配 AUTOSAR、会写 C"跨到"懂电池算法"的底层工程师。讲清楚三件事：电池状态**为什么只能估算不能测量**、主流算法**原理与公式**、以及**怎么用 Simulink 把算法建模并生成可上车的 C 代码**。

## 1. BMS 算法全景：到底在估什么

电池是一个"黑箱"——你能量到的是端电压 `U`、总电流 `I`、各点温度 `T`，但真正关心的是**内部状态**，没有传感器能直接读出。BMS 算法干的事，就是从可测量里反推这些内部状态。

| 状态量 | 含义 | 能否直接测 | 用途 |
|---|---|---|---|
| SOC | 荷电状态（剩余电量比例） | 否 | 续航显示、功率限制、均衡触发 |
| SOH | 健康状态（容量/内阻衰退） | 否 | 可用能量、保修、报废判定 |
| SOP | 功率状态（最大可充/放功率） | 否 | 整车功率分配、防过流 |
| SOE | 能量状态（剩余可用能量） | 否 | 里程预估（比 SOC 更准） |
| T 场 | 内部温度分布 | 只能测表面 | 热管理、安全边界 |

> 关键认知：**SOC/SOH 是"估算"不是"测量"**。这决定了所有算法都是"模型 + 观测 + 校准"的结构，永远带误差，工程上追求的是把误差压到可接受区间（车规通常要求全温域 SOC 误差 ≤ 5%，50% 附近 ≤ 3%）。

算法在软件栈里的位置（自底向上）：`AFE 采样 → 数字滤波/校准 → 状态估算(SOC/SOH/SOP) → 决策(均衡/热管理/故障) → 通过 Com/RTE 上报`。和前面 AUTOSAR 章节的 NvM 配合：学习值、SOC 偏移、SOH 都靠 Fee/NvM 掉电保存。

## 2. 电池建模：所有估算的物理地基

没有模型，滤波器和观测器都无从谈起。工程最常用**等效电路模型（ECM）**，用电阻电容网近似电池动态。

| 模型 | 结构 | 精度 | 计算量 | 适用 |
|---|---|---|---|---|
| Rint | 1 个 OCV + 1 个内阻 R0 | 低（无极化） | 极小 | 粗略 SOC、教学 |
| Thevenin(1RC) | OCV + R0 + 1 个并联 RC(Rp/Cp) | 中 | 小 | 车载主流 SOC/EKF |
| 2RC | OCV + R0 + 2 个 RC | 高 | 中 | 功率/动态工况 |
| PNGV | 含电容表示开路电压漂移 | 高 | 中 | 长时间尺度 |
| 电化学模型(DFN/P2D) | 偏微分方程 | 极高 | 大 | 离线标定、SOH 研究 |

**1RC 模型状态方程**（最常用，务必背熟）：

- 开路电压：`Uocv = f(SOC)`，由 OCV-SOC 查表得到
- 极化电压动力学：`Cp · dUp/dt = I - Up/Rp`，离散化得 `Up[k] = Up[k-1]·exp(-Ts/(Rp·Cp)) + I[k]·Rp·(1 - exp(-Ts/(Rp·Cp)))`
- 端电压观测：`U[k] = Uocv(SOC[k]) + I[k]·R0 + Up[k]`

> 注意 OCV-SOC 有**滞回**：充电曲线和放电曲线不重合（尤其 LFP 不明显、NCM 明显）。严格模型要在 OCV 里加滞回项 `Uocv = f(SOC) + h·ΔUhyst`，否则 SOC 在充放电切换时会跳变。

**参数怎么来**：对真实电芯做 HPPC 脉冲（充/放一阵停一阵）或 GITT（小电流阶梯），用电压台阶反推 R0、Rp、Cp；OCV 用极小电流（C/20 以下）充满放空扫曲线。这些参数**随温度、随老化变化**，所以量产车要做温度查表，甚至在线辨识。

## 3. SOC 估算：最核心也最常被问

### 3.1 定义与两个积分法
SOC 定义为 `SOC = (Q_max - Q_used) / Q_max`，其中 `Q_used = ∫I(t)dt`（放电为正）。互补量叫 DOD（Depth of Discharge），`DOD = 1 - SOC`。

**安时积分法（Coulomb Counting）**——量产里一定有的基线：
`SOC[k] = SOC[k-1] - I[k]·Ts / (Q_max·3600)`，电流单位 A、时间 s、容量 Ah。

- 优点：实时、简单、不依赖模型。
- 致命缺点：**误差累积**。电流采样偏置 `I_offset` 会让 SOC 一直漂；温度/倍率影响有效容量；初始值不知道只能猜。
- 补救：定期用 OCV 或满充/放空"锚定"校准（见 3.4）。

**OCV 查表法**：电池静置足够久（极化消失）后，端电压≈OCV，反查 OCV-SOC 表得 SOC。慢、要静置，但**无累积误差**，常用来校准安时积分。

### 3.2 卡尔曼滤波（EKF）：把两者融合的标准答案
EKF 是车载 SOC 的工业主流。它把 1RC 模型变成离散状态空间：

- 状态 `x = [SOC, Up]^T`，输入 `u = I`，观测 `z = U`
- 预测：`x̂[k|k-1] = f(x̂[k-1], u[k])`（用上面的离散方程）
- 观测：`z[k] = h(x̂) + v`，`h = Uocv(SOC) + I·R0 + Up`
- 计算雅可比 `F`、`H` → 预测协方差 `P` → 卡尔曼增益 `K = P·Hᵀ·(H·P·Hᵀ+R)⁻¹` → 更新 `x̂ = x̂ + K·(z - h(x̂))`

> 直觉：EKF 用"模型预测"和"实测电压"两个来源做加权融合，模型不准就用电压纠，电压有噪就用模型平滑，比单用安时积分稳得多。它**天然能处理噪声和初始误差收敛**。

**UKF（无迹卡尔曼）**：不用雅可比，用"sigma 点"逼近非线性，对强非线性（大电流、低温）更稳，计算略贵。**粒子滤波**：非高斯强、最贵，多在论文。面试能说出"EKF 主流、UKF 更准、PF 最贵但最准"就够。

### 3.3 温度/倍率/老化补偿
- 容量 `Q_max` 随温度下降（低温有效容量小），要做 `Q_max(T)` 查表。
- 内阻随低温飙升，OCV 也漂移，统一进模型参数温度表。
- 老化后 `Q_max` 变小、`R0` 变大，这就是 SOH 要喂给 SOC 的输入。

### 3.4 初始 SOC 校准策略（量产必答）
- **充电锚定**：判定充满（电流<阈值且电压到上限一段时间）→ SOC 强制=1。
- **放电锚定**：放到截止电压 → SOC=0。
- **静置 OCV 锚定**：车辆长时间静置 → 用 OCV 表直接覆写。
- **行车中在线收敛**：EKF 在行驶中靠电压观测逐步把初始误差吃掉。

> 工程坑：低温下 OCV 很平（尤其 LFP 平台区），静置校准误差极大；安时积分的 ADC 偏置必须上电自校准，否则一晚上漂几个点。

## 4. SOH 估算：容量与内阻的衰退

### 4.1 定义
- 容量法：`SOH_Q = Q_now / Q_init`（Q_init 标称，Q_now 当前满充容量）
- 内阻法：`SOH_R = R_init / R_now`
- 两者不等价：容量衰减直接砍可用能量，内阻增长主要砍功率能力和发热。业内常报 `SOH = min 或 加权` 二者。

### 4.2 老化机理（聊明白物理）
- **SEI 膜增长**：负极表面消耗活性锂，不可逆 → 容量掉。
- **活性物质损失**：循环应力使电极颗粒裂、锂 plating（低温快充最容易）→ 容量掉、内阻升。
- **日历老化**：高温静置自衰减，比循环更隐蔽。

### 4.3 估算方法（由易到难）
- **满充容量跟踪**：每次判定充满，统计本次从某 SOC 到满的安时量，反推当前 `Q_now`。简单可靠，是量产首选。
- **内阻法**：在固定工况（如某 SOC 脉冲）测 `R0`，对比初始值。
- **模型参数法**：把 SOH 当状态量一并进 EKF/联合观测。
- **增量容量 ICA（dQ/dV）**：对充电曲线求导，特征峰的位置/面积随老化移动，能定位是哪种衰减。配合 **DV（微分电压）** 做在线老化诊断，是当前研究热点。

> SOH 变化极慢（几百到几千循环），所以**不需要实时**，每次满充或定期更新一次即可，存进 NvM。

## 5. 均衡（BAL）：把不一致的电芯拉齐

### 5.1 为什么不均衡不行
电芯出厂就有容量/内阻离散，使用中自放电、温区不同进一步放大。后果：最弱那颗先到截止电压 → 整包被迫停止充放 → 可用容量被木桶短板锁死。

### 5.2 被动 vs 主动（核心对比）

| 维度 | 被动均衡（能耗） | 主动均衡（能量转移） |
|---|---|---|
| 原理 | 给高电量电芯并联电阻放电耗掉 | 把高电量电芯的能量搬给低的 |
| 拓扑 | 电阻 + MOS | 电容/电感/变压器（反激、Buck-Boost） |
| 效率 | <100%（纯浪费） | 80%~90% |
| 速度 | 慢（mA 级放电） | 快（可 A 级） |
| 成本/复杂度 | 极低 | 高（器件多、控制难） |
| 发热 | 在电芯旁发热，需控温 | 集中，相对好控 |
| 适用 | 低端、小包、慢充 | 高端、大容量、快充 |

> 被动均衡控制简单（开 MOS 放电到阈值差以内），但浪费能量且慢；主动均衡效率高、快，但拓扑和算法复杂，量产可靠性挑战大。乘用车主流仍是被动，商用车/储能开始上主动。

### 5.3 均衡判据与策略
- **判据**：电压差 `ΔU > 阈值`、或 SOC 差 `ΔSOC > 阈值`、或容量差。电压阈值最简单但受极化干扰，SOC 差最准但依赖 SOC 精度。
- **触发**：充电末端（顶部均衡，被动常用）、放电末端、或定时按需。
- **主动控制算法**：相邻转移（只和邻居换）、排序转移（最高给最低）、基于 Cauer 网络的集中调度。
- **时间估算**：`t = ΔQ / I_bal`，电阻功率 `P = I_bal²·R`，要算热预算别烧板。

> 安全：均衡不能让任何电芯过放；被动均衡的电阻功率要留余量，BMS 要监控均衡 MOS 温度。

## 6. 其它关键算法（面试常带一嘴）

- **SOP 功率状态**：在 SOC 上下限、电压限、电流限、温度限共同约束下，解"此刻最大能放/充多少功率、能维持多久"。公式化就是带约束的优化，常用查表+线性近似。
- **SOE 能量状态**：`SOE = 可用能量 / 标称能量`，比 SOC 更贴近真实续航（考虑了老化后缩水的容量）。
- **故障诊断**：过压/欠压/过流/过温/低温/绝缘阻值/温感开路，喂给 DEM 记 DTC，和 AUTOSAR DEM 那套对齐。
- **热管理**：根据温升模型决定开风扇/水泵/加热膜，低温先加热再快充（防锂 plating）。

## 7. Simulink 建模与代码生成（底层工程师也能上手）

> 这一节是重点：你配过 AUTOSAR、写过 C，但要搞懂"算法是怎么从框图变成车规 C 代码的"。Simulink 在这里是**算法原型 + 自动代码生成**的工具，不是单纯画仿真图。

### 7.1 为什么 BMS 算法用 Simulink
- 状态方程、卡尔曼增益、均衡调度都是数学，图形化建模不容易写错、好和算法工程师对齐。
- **Embedded Coder 一键生成 C**：模型即代码，避免手写数值代码的笔误，且可复现。
- **MIL/SIL/PIL/HIL 验证闭环**：同一套模型，先仿真验证（MIL），再跑生成的代码（SIL），再上板（PIL），结果逐层比对，功能安全（ISO 26262）喜欢这套可追溯性。

### 7.2 工具与环境
MATLAB + Simulink + `Simscape Electrical`（自带电池模块）/ `Stateflow`（均衡状态机）/ `Embedded Coder`（生成 C，可配 `autosar.tlc` 直接出 AUTOSAR 接口）。

### 7.3 建模四步（以 SOC EKF 为例）
1. **建电池模型**：用 Simscape 电池块，或自己搭 1RC（Integrator + 乘法 + 查表 `Uocv(SOC)`）。
2. **写算法**：用 `MATLAB Function` 块实现 EKF 预测/更新，或用 `Stateflow` 画均衡状态机（闲置→检测→均衡→完成）。
3. **配接口**：输入 `I, U, T`，输出 `SOC, SOH, 均衡命令`；设固定步长 `Ts=0.01s`（10ms 任务）。
4. **生成代码**：`Embedded Coder → Build`，选 `ert.tlc`（或 `autosar.tlc`）。

### 7.4 生成的 C 代码长什么样
生成后大致是这种结构（示意）：
`extern void BMS_step(void);` —— 每个任务周期调用一次
`typedef struct { real32_T SOC; real32_T SOH; real32_T Up; } BMS_DW;` —— 状态变量打包成结构体
`BMS_DW 机体;` —— 全局状态（不靠栈，防重入问题）
函数体里就是 `机体.SOC = 机体.SOC - I * Ts / Qmax;` 这种浮点运算 + 查表 + 矩阵乘法（卡尔曼增益）。

> 你写裸 C 也能实现同样逻辑，但 Simulink 保证**数值结构、定点、接口、可追溯性**一致，且改模型重生成即可，不用手改几百行 C。对底层工程师的价值：你能读生成代码、能在 `BMS_step()` 外包一层被 10ms 任务调用、能把它映射成 AUTOSAR Runnable。

### 7.5 数据类型与定点化
- 默认 `double`，车载 MCU 常用 `single` 或**定点 `fixdt(1,16,10)`**（1 符号位、16 总长、10 小数位）。
- 定点要防**溢出**和**精度损失**：卡尔曼协方差可能很大，状态很小，定标要分开；Embedded Coder 能自动分析范围给建议定标。
- 这点和你写 C 时的 `float`/`int16` 取舍一脉相承，只是模型里可视化了。

### 7.6 集成到 AUTOSAR（和你前面学的接起来）
- 用 `autosar.tlc` 生成时，可把 `BMS_step` 配成 **Runnable**，I/O 配成 **Sender/Receiver Port** 或 **NvM 接口**。
- 调度：RTE 在 10ms 周期 Task 里调这个 Runnable；采样的 `I/U/T` 来自 COM/RTE 传入，算完 `SOC` 通过 RTE 发出去给 VCU/显示屏。
- 掉电保存：SOC 偏移、SOH、学习值经 NvM→Fee→Fls（前面 Flash 章节那条链）写进 EEPROM。

### 7.7 验证闭环（功能安全最爱）
- **MIL**（Model-in-Loop）：模型跑测试向量，看 SOC 曲线对不对。
- **SIL**（Software-in-Loop）：编译生成代码在 PC 上跑同一向量，和 MIL 比，误差应为 0（证明生成没走样）。
- **PIL**（Processor-in-Loop）：代码交叉编译烧到 S32K144 跑，回传结果比，验证定点/编译器差异。
- **HIL**：接真实控制器+电池模拟器做系统测试。

> 最小可练：在你 S32K144 上，用 Simulink 建一个"安时积分 SOC"模型 → 生成 C → 接到 10ms 任务 → CAN 把 SOC 发出去看。这一步打通，SOC EKF、均衡状态机都是它的加厚版。

## 8. 工程坑与面试要点

- 电流 ADC **偏置不校准** → 安时积分一晚漂 5%，必须上电自校零。
- **低温 SOC 误差大**：OCV 平、容量缩水、内阻飙，三重叠加，要做温度补偿+低温谨慎策略。
- **均衡过度发热**：被动均衡电阻功率要算余量，BMS 要监控 MOS 温度。
- **模型参数会漂**：R0/Rp/Cp 随温度老化变，长期要在线辨识或温度查表。
- **OCV 滞回没处理** → 充放电切换 SOC 跳变，体验差。
- 面试高频题：SOC 有哪几种方法、为什么用 EKF、主动被动均衡区别、SOH 怎么估、Simulink 怎么生成车规代码、定点化为什么、MIL/SIL/PIL 是什么。

> 和你简历的衔接：单季度电池包出货破万的量产项目里，SOC 精度、均衡策略、故障诊断都是**可量化亮点**。把"用 EKF+OCV 融合把全温域 SOC 误差压到 X%"或"被动均衡策略把电芯一致性控制在 Y mV"这类话术组织好，比泛泛说"做过 BMS"有力得多。

## 9. 与题库标签衔接

- 关联标签：`bms` / `algorithm`（算法）/ `simulink`（建模代码生成）/ `soc` / `soh` / `bal`（均衡）。
- 和前面章节的上下游：电池模型参数 → Flash 章节的 Fee/NvM 掉电保存；SOC Runnable → AUTOSAR OS 章节的任务调度；故障诊断 → DEM/DCM 体系。
- 实操入口：照 `S32K144_上手指南.md` 把 Simulink+Embedded Coder 环境跑通，从"安时积分 SOC → 生成 C → CAN 发出"这条最小链路做起。
