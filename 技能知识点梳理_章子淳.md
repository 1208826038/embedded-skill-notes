# 嵌入式底层软件工程师 · 技能知识点梳理

> 梳理人：章子淳 ｜ 方向：嵌入式底层软件 / BSW / 新能源汽车（BMS）底层软件
> 说明：本梳理基于本人简历中的项目与技术栈，按"我会什么 → 核心知识点 → 面试易问"组织，用于面试复习与知识自测。

---

## 一、ARM Cortex-R/M 内核与底层架构

**我会什么**
- 深入理解 ARM Cortex-R/M 内核架构：异常模型、Cache、TCM、MPU
- 具备新芯片平台底层移植与 AUTOSAR BSW 框架搭建能力

**核心知识点**
- **异常 vs 中断**：异常是 CPU 同步/异步的统称（含中断、SVC、Fault），中断是异常的一种；Cortex-M 用 NVIC 管理，Cortex-R 用 VIC/中断向量表
- **Cache**：指令/数据缓存，加速取指与访存；需关注一致性（DMA 改写内存后 Invalid Cache）
- **TCM（Tightly Coupled Memory）**：紧贴内核的确定性低延迟内存，不进 Cache，用于中断向量、关键栈、实时任务指令
- **MPU（Memory Protection Unit）**：把内存分区、设访问权限（R/W/X），越权触发 MemManage Fault；是单核实现功能安全隔离的关键
- **AUTOSAR BSW**：基础软件分层（MCAL / ECU 抽象 / 服务层），MCAL 直接操作硬件

**面试易问**
- 异常和中断的区别？
- 为什么实时任务代码放 TCM 而不是普通 RAM？
- MPU 和 MMU 的区别？没有 MMU 怎么保证隔离？

---

## 二、RTOS 与系统调优

**我会什么**
- 熟练使用 RTOS，具备 CPU Loading 分析、任务调度优化、多核协同实战经验
- 修复过 RTOS 核心 Bug（中断标志位数据类型错误导致报文抖动与 SPI 延迟）
- 将 CPU Loading 从 80%+ 降至约 60%

**核心知识点**
- **任务调度**：抢占式优先级调度；优先级分配按实时性需求（关键任务高优先级）
- **CPU Loading 分析**：统计各任务占用率，定位热点
- **降负载组合拳**：软件 IIC → 硬件 IIC（免 CPU 轮询）、全通道 DMA（ADC/SPI 免中断搬运）、ITCM/DTCM 放热路径、开启硬浮点（FPU 不进软模拟）、调编译优化等级（-O2）
- **任务切换 Bug 根因**：中断标志位用错数据类型（如 8 位截断 32 位标志）→ 切换时机错 → 报文周期抖动、SPI 响应延迟

**面试易问**
- 你怎么定位 CPU Loading 高？
- 开硬浮点有什么坑？（ABI 一致性、中断上下文 FPU 现场保护）
- 任务优先级怎么定？优先级反转怎么避免（优先级继承）？

---

## 三、功能安全（ISO 26262）

**我会什么**
- 具备 MPU 功能安全隔离（ISO 26262）实战经验
- 为 ASIL D 级安全任务设置独立内存保护区域，防止非安全任务越权访问

**核心知识点**
- **ASIL 等级**：A→D 风险递减，D 最严（失效可接受概率最低）
- **隔离机制**：MPU 分区 + 编译期内存分配 + 看门狗/ECC；单核无 MMU 靠 MPU 实现空间隔离
- **流程要求**：需求追溯、单元测试、静态分析、故障注入、安全分析（FMEA/FTA）

**面试易问**
- ASIL D 和 ASIL B 工作量差多少？
- 单核 MCU 怎么做到 ASIL D 的空间隔离？
- 什么是自由执行时间（Freedom From Interference）？

---

## 四、MCAL 驱动开发

**我会什么**
- 深入掌握 ADC（DMA + 硬件触发）、CAN 等模块配置与驱动开发
- 跨 6 款芯片平台（瑞萨/英飞凌/芯驰/国芯/紫光/恩智浦）外设驱动移植
- 多款外设芯片（高压采样、RTC、CAN 收发器）HAL 驱动开发

**核心知识点**
- **ADC + DMA + 硬件触发**：硬件触发启动采样，DMA 自动搬运结果，免 CPU 干预、降低抖动；采样时钟/周期需满足信号带宽（建立时间不足→精度下降）
- **高压采样**：精度靠采样时序配置、内部校准、通道顺序避开串扰
- **PWM**：可调频率/占空比，用于保险丝引爆等场景；分辨率由定时器位数决定
- **ICU（输入捕获）**：精确测量外部信号频率/占空比，如碰撞信号检测
- **HAL 驱动**：按芯片手册配通信时序，逻辑分析仪波形验证

**面试易问**
- ADC 采样精度怎么保障？
- 采样周期太短会怎样？
- PWM 和 ICU 分别用于什么场景？

---

## 五、通信协议（CAN / CAN FD / SPI / IIC）

**我会什么**
- 精通 CAN/SPI/IIC 协议栈开发与调试
- 设计 CAN/CAN FD 动态兼容方案（标定变量切换 FD 标志，一套代码覆盖双协议）

**核心知识点**
- **CAN**：11/29 位 ID、8 字节数据、仲裁机制（ID 小优先）、位填充
- **CAN FD**：数据段最长 64 字节、数据段速率最高 8Mbps（仲裁段仍低速）；解决传统 CAN 带宽瓶颈
- **动态兼容**：标定变量作报文类型判断位，写硬件控制器时动态设 FD 帧标志 → 同一代码库切换双协议，消除双版本维护
- **SPI/IIC**：板级低速通信，逻辑分析仪/示波器抓波形定位时序问题

**面试易问**
- CAN FD 和 CAN 帧差异？为什么需要 FD？
- 仲裁段和数据段速率为何能不同？
- 一套代码不重新编译怎么切换 CAN/CAN FD？

---

## 六、编译与链接（GHS / Tasking）

**我会什么**
- 精通 GHS (MULTI IDE)、Tasking 编译器及链接脚本
- 熟练解决 Section 溢出、Symbol 冲突、对齐约束
- 多芯片内存布局设计

**核心知识点**
- **链接脚本**：划分 RAM/Flash 区域，指定各段（.text/.data/.bss）落位，分配模块/任务栈空间
- **Section 溢出**：段超出分配内存；看 map 文件定位，挪区/改对齐/拆分/开链接优化
- **Symbol 冲突**：同名全局变量、弱符号冲突、不同库符号重名
- **对齐约束**：结构体/变量对齐影响访问效率与硬件 DMA 要求

**面试易问**
- Section 溢出怎么定位与修？
- Symbol 冲突一般怎么来的？
- 链接脚本谁写？你怎么给多核分内存？

---

## 七、工具链自动化与工程效率

**我会什么**
- Python/Shell 脚本开发，自主开发自动化代码生成工具替代商用软件
- 利用 AI 工具协助编写 Python 脚本解析 DBC 文件，自动生成通信配置代码，替代 DaVinci Configurator/Developer
- 日常报文配置时间由一天缩短至半小时

**核心知识点**
- **DBC**：CAN 数据库，描述报文（ID/DLC/周期）与信号（起始位/长度/因子/偏移/单位/字节序）
- **代码生成**：解析 DBC → 生成报文收发与信号编解码代码，省去手工配置
- **字节序**：Motorola（大端，跨字节信号高位在前）vs Intel（小端）信号布局差异

**面试易问**
- DBC 里有什么？
- 生成的代码主要做什么？
- Motorola 和 Intel 字节序信号怎么处理？

---

## 八、国产芯片替代与硬件联调

**我会什么**
- 主导国产芯片平台准入技术评审，组织原厂/代理商技术对齐
- 攻克国产芯片替代关键技术（TJA1145 国产替代 SPI 波形差异、唤醒源延迟）
- SBC 下电时序优化（电容放电 400us vs MCU PWD 200us 错配导致复位）

**核心知识点**
- **替代验证**：示波器/逻辑分析仪抓时序，对比原厂与国产芯片行为差异
- **SBC 下电 Bug**：休眠时 SBC 电容放电慢于 MCU 进低功耗，导致 MCU 被意外复位；调时序让 MCU 先稳定进低功耗
- **根因分析**：从现象（偶发休眠失败/复位）→ 波形定位 → 机制修复（延迟确认、状态清理）

**面试易问**
- 国产替代踩过什么坑？
- 你怎么验证替代芯片"等价"？
- 怎么测出 400us vs 200us 的时序差异？

---

## 九、跨团队协同与需求管理

**我会什么**
- 牵头客户需求输入评审会，形成书面需求基线（SOR）
- 主导芯片平台准入评审，推动多款芯片通过准入并完成量产适配

**核心知识点**
- **需求基线 SOR**：逐条确认通信/功能安全/实时性需求，形成书面文档，源头规避后期变更返工
- **供应商协同**：与原厂/代理商技术对齐，明确电气特性、质量交付、长期供货能力

**面试易问**
- 如果原厂说"不支持某个特性"你怎么处理？
- 需求变更你怎么控制？

---

## 速记清单（面试前 10 分钟过一遍）

1. CPU Loading 80→60 的五板斧：硬件 IIC / DMA / TCM-DTCM / 硬浮点 / -O2
2. ASIL D 隔离：MPU + 编译期分区 + 看门狗/ECC
3. CAN FD：64 字节、数据段 8Mbps、动态 FD 标志切双协议
4. ADC 精度：采样时序 + DMA 硬件触发 + 校准 + 通道顺序
5. 链接脚本：map 文件定位 Section 溢出，挪区/对齐/拆分
6. DBC：报文+信号，生成编解码代码；Motorola vs Intel 字节序
7. SBC 下电：放电 400us vs PWD 200us → 调时序
8. 国产替代：示波器/逻辑分析仪抓波形差异
9. 需求基线 SOR：书面化，源头防返工
10. BMS 底层标签：ADC 高压采样 / PWM 保险丝引爆 / CAN 通信 都串得起
