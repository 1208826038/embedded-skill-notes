# 嵌入式外设协议详解（笔者）

> 方向：嵌入式底层软件 / BSW / 新能源汽车（BMS）底层软件
> 说明：本文系统梳理车载与嵌入式开发中常用的**外设/通信协议**，每个协议按"物理层 → 帧格式/时序 → 典型应用 → BMS/汽车场景 → 常见坑与调试 → 面试易问"展开，用于底层驱动开发与面试复习。重点覆盖 BMS 相关的 CAN/CAN FD、SPI、I²C/SMBus、DSI3 等。

---

## 一、CAN / CAN FD（控制器局域网）

**定位**：车载主干总线，高可靠、强实时、多主竞争、差分抗干扰。

### 1. 物理层
- 双绞线差分：**CANH / CANL**，显性(逻辑0)=两线压差约 2V，隐性(逻辑1)=压差约 0V。
- 总线两端各 **120Ω 终端电阻**，消除信号反射；支线(stub)尽量短。
- 速率：CAN 最高 1Mbps（典型 500k/250k）；CAN FD 仲裁段 ≤1Mbps，数据段最高 8Mbps。
- 收发器：如 TJA1145（带休眠/唤醒/看门狗），国产替代需比对 SPI 配置波形与唤醒延迟。

### 2. 逐字段详解（标准帧 / 扩展帧 / FD 帧）

**标准帧（11 位 ID）完整位结构**
```
SOF(1) | ID(11) | RTR(1) | IDE(1) | r0(1) | DLC(4) | DATA(0~8B) | CRC(15+1) | ACK(1+1) | EOF(7)
```
**扩展帧（29 位 ID）完整位结构**
```
SOF(1) | ID(11) | SRR(1) | IDE(1) | ID(18) | RTR(1) | r1(1) | r0(1) | DLC(4) | DATA(0~8B) | CRC(15+1) | ACK(1+1) | EOF(7)
```
**CAN FD 帧（标准帧基础上复用保留位）**
```
SOF | ID(11) | RTR(恒0) | IDE(1) | FDF(1) | rsv(1) | BRS(1) | ESI(1) | DLC(4) | DATA(0~64B) | CRC(17/21+1) | ACK | EOF
```
> 注：FD 把原经典帧的保留位 r0 复用为 FDF，并在其后新增 rsv/BRS/ESI，体现"向后兼容、旧控制器遇 FDF=1 会当错误帧处理"。

#### 每个字段的含义

| 字段 | 位宽 | 取值 | 含义 |
|------|------|------|------|
| **SOF** | 1 | 显性(0) | 帧起始（Start of Frame），总线空闲(隐性)后第一个显性位，所有节点据此同步 |
| **ID** | 11/29 | — | 报文标识符 = 报文内容 + 优先级（**不是地址**）；仲裁段逐位比较，数值越小优先级越高 |
| **SRR** | 1 | 隐性(1) | 仅扩展帧，Substitute Remote Request，保证"前 11 位 ID 相同"时扩展帧仲裁弱于标准帧 |
| **RTR** | 1 | 数据帧0 / 远程帧1 | Remote Transmission Request；远程帧无数据段、用于请求数据。CAN FD 中恒为显性(0)，FD 无远程帧 |
| **IDE** | 1 | 0=标准 / 1=扩展 | Identifier Extension，标识帧类型 |
| **r0 / r1** | 1 | 显性(0) | 保留位，留作协议演进；CAN FD 中部分被复用为 FDF / BRS / ESI |
| **FDF** | 1 | 0=经典 / 1=FD | FD Format，FD 格式标志（由经典帧的 r0 位复用） |
| **BRS** | 1 | 0=不切 / 1=切 | Bit Rate Switch，数据段是否切换到高速率（仅 FD） |
| **ESI** | 1 | 0=主动 / 1=被动 | Error State Indicator，发送节点错误状态（被动错误时发隐性，提示网络异常） |
| **DLC** | 4 | 0~8 / FD 0~64 | Data Length Code，数据长度码；FD 的 DLC 编码**非线性**（见下） |
| **DATA** | 0~8 / 64B | — | 应用数据，内部信号布局由 DBC 定义（起始位/长度/字节序） |
| **CRC** | 15 / 17 / 21 | — | 循环冗余校验；经典 15 位，FD ≤16 字节用 17 位、>16 字节用 21 位，后跟 1 位 CRC 定界符 |
| **ACK** | 1+1 | — | 应答槽 + 应答定界符；发送方发隐性，任意正确接收节点覆写为显性表示"已收到" |
| **EOF** | 7 | 隐性(1) | End of Frame，帧结束，期间禁止启动新帧，给错误帧/过载帧留窗口 |

**DLC 编码对照（CAN FD 非线性，经典 CAN 仅 0~8）**

| 字节数 | 0 | 1 | 2 | 3 | 4 | 5 | 6 | 7 | 8 | 12 | 16 | 20 | 24 | 32 | 48 | 64 |
|--------|---|---|---|---|---|---|---|---|---|----|----|----|----|----|----|----|
| DLC 码 |0x0|0x1|0x2|0x3|0x4|0x5|0x6|0x7|0x8|0xC|0xD|0xE|0x1|0x2|0x3|0x4|

> 即 9~11、13~15、17~19、21~23、25~31、33~47、49~63 这些 DLC 值**不允许出现**。

#### 每个字段的对应面试题

- **SOF**：帧起始为什么用显性(0)？→ 总线空闲是隐性(1)，SOF 用显性可在空闲后产生明确下降沿，所有节点据此对齐采样点、完成硬同步。
- **ID**：① CAN 没有目标地址怎么通信？→ 内容寻址：报文按 ID 标识含义，节点用验收滤波器(mask+code)决定是否接收，而非"发给某地址"。② 为什么 ID 越小优先级越高？→ 仲裁从 ID 最高位逐位比，显性(0)赢；数值越小高位越多 0，更早赢得总线。③ 11 位和 29 位怎么选？→ 标准帧 ID 空间小(2048)、解析简单、实时性好；扩展帧用于信号密集/网关路由需要更多 ID 的场景。
- **SRR**：扩展帧为什么要有 SRR？→ 在标准帧 11 位 ID 与扩展帧前 11 位 ID 相同时，SRR(隐性) 让扩展帧在仲裁中"先输"，保证标准帧优先，消除二义。
- **RTR**：远程帧是什么？→ 网络节点用来请求其他节点发送某数据的帧，自身不含数据段；RTR=1(隐性)。CAN FD 已取消远程帧（RTR 恒显性）。
- **IDE**：IDE 位如何区分帧类型？→ IDE=0 标准帧，IDE=1 扩展帧；仲裁阶段据此切换后续位宽。
- **r0 / r1（保留位）**：保留位有什么用？→ 给协议未来演进预留；CAN FD 正是复用经典帧的保留位(r0→FDF 等)实现兼容升级，旧控制器遇到 FDF=1 会当错误帧处理而不误判。
- **FDF**：怎么区分经典 CAN 与 CAN FD 帧？→ 看 FDF 位（原 r0 位置）：FDF=0 经典帧，FDF=1 FD 帧。
- **BRS**：为什么数据段能切换速率？→ BRS=1 时，仲裁段结束后比特率切到高速（如 2/5/8Mbps），数据段结束前切回仲裁速率；仲裁段保持低速以保证远距离可靠同步。
- **ESI**：ESI 错误状态指示有什么用？→ 被动错误节点发隐性 ESI，向网络广播"我已进入错误被动状态"，便于故障定位与隔离，避免其持续干扰通信。
- **DLC**：① 为什么 4 位 DLC 能表达 64 字节？→ FD 采用非线性编码，把 12/16/20/24/32/48/64 映射为特定码值(0xC/0xD/0xE/0x1/0x2/0x3/0x4)。② DLC 与 CRC 位宽有什么关系？→ 数据越长需更强校验，FD 按数据是否超过 16 字节选用 17 位或 21 位 CRC。
- **DATA**：① 数据段最大多少？→ 经典 CAN 8 字节，CAN FD 64 字节。② 数据里的信号怎么排布？→ 由 DBC 定义每个信号的起始位、长度、字节序(Motorola/Intel)与因子偏移，驱动层据此 pack/unpack。
- **CRC**：① 为什么 FD 要把 15 位 CRC 加到 17/21 位？→ 数据段从 8 字节扩到 64 字节，短 CRC 的汉明距离/检错概率不足，加长以保传输可靠性。② CRC 校验失败会怎样？→ 接收方不回 ACK、发出错误帧，发送方检测到后自动重发。
- **ACK**：① ACK 机制是什么？→ 发送方在 ACK 槽发隐性(1)，总线上任意正确接收节点把它覆写为显性(0)，表示"我收到了"。② ACK 缺失说明什么？→ 总线上没有节点正确接收（断线、波特率不匹配、无节点滤波器匹配该 ID）。
- **EOF**：EOF 为什么是 7 位隐性且期间禁止发新帧？→ 明确帧边界、给错误帧/过载帧保留间隔窗口，防止两帧重叠导致连续错误。

#### 帧级机制（仲裁 / 位填充 / 错误检测）

- **仲裁**：ID 越小优先级越高；非破坏性逐位仲裁（输的一方自动退出发送，不丢数据）。
- **位填充**：连续 5 个相同位后插入 1 个反相位，保证足够的边沿用于同步；接收端自动去填充。
- **错误检测**：位监控、格式检查、CRC、ACK 缺失、位填充违例 → 自动重发。

### 3. CAN FD 增强
- **FDF** 标志区分 FD 帧；**BRS**（Bit Rate Switch）在数据段切换高速率；数据段 **64 字节**；**ESI** 错误状态指示。
- 仲裁段仍低速以保证远距离可靠仲裁，进入数据段后提速 → 解决传统 CAN 带宽瓶颈（标定/刷写场景明显）。
- **动态兼容方案**：用标定变量作报文类型判断位，写控制器时动态设 FD 标志 → 一套代码覆盖 CAN/CAN FD 双协议，消除双版本维护。

### 4. 位时序（Bit Timing）
- 一位 = 同步段(SYNC_SEG) + 传播段(PROP_SEG) + 相位缓冲段1/2(PSEG1/2)，采样点通常设在 75%~87.5%。
- 参数：**波特率**、**SJW**（同步跳转宽度，抗时钟偏差）、**采样点**三者需全网一致，否则通信不稳。

### 5. BMS/汽车场景
- BMS 与整车 CAN 通信：上报电压/温度/SOC、接收充电/放电指令、故障告警。
- 诊断用 CAN（DoIP 之前仍大量走 CAN 跑 UDS）。

### 6. 常见坑
- 终端电阻缺失/不符 → 反射、波形过冲、通信丢包。
- 波特率/采样点全网不一致 → 偶发错误帧。
- 收发器供电/唤醒时序错配（见 SBC 下电专题）→ 休眠失败/复位。
- 总线负载过高 → 仲裁延迟、实时性下降。

### 7. 面试易问
- CAN 为什么用差分？→ 抗共模干扰、长距离可靠。
- 仲裁为什么非破坏性？→ 输方自动退出，高优先级报文无延迟送达。
- CAN FD 为什么数据段能提速？→ 仲裁完成后双方已同步，数据段可换高速率。
- 一套代码不重新编译怎么切 CAN/CAN FD？→ 运行时由标定变量动态设 FD 标志。

---

## 二、LIN（本地互联网络）

**定位**：低成本车身子网，单线、主从、确定调度。

### 1. 物理层
- **单线** (+12V 供电线复用)，速率 1~20kbps（典型 19.2k）。
- 主节点拉电阻上拉到电池，从节点用开源/开漏驱动。

### 2. 逐字段详解（LIN 帧）

**完整帧结构**
```
Break(≥13位0) | BreakDelim(≥1位1) | Sync(0x55) | PID(8) | DATA(1~8B) | Checksum(8)
```

| 字段 | 位宽 | 取值 | 含义 |
|------|------|------|------|
| **Break** | ≥13 位 | 显性(0) | 帧起始，拉低总线≥13 位通知所有从节点"新帧来了" |
| **Break Delimiter** | ≥1 位 | 隐性(1) | 分隔 Break 与 Sync，保证 Sync 首位的上升沿可被采样 |
| **Sync** | 8 位 | 0x55 (01010101) | 同步字节，从节点据此测出主节点波特率（数下降沿间隔） |
| **PID** | 8 位 | — | Protected ID = 6 位帧 ID(0~63) + 2 位奇偶校验(parity) |
| **DATA** | 8~64 位 | — | 1~8 字节数据，低字节先发、字节内 LSB 先发 |
| **Checksum** | 8 位 | — | 校验和；经典(对数据异或) / 增强(对 PID+数据异或) |

**校验算法**
- 经典校验(Classic)：所有数据字节累加(模256)取反 → 不含 PID。
- 增强校验(Enhanced)：PID + 所有数据字节累加取反 → 用于诊断类等需更高可靠的帧。

#### 每个字段的对应面试题
- **Break**：为什么 Break 要≥13 位显性？→ 远超正常字节（带填充最多 9 位连续相同），确保从节点从休眠/任意状态可靠识别为帧起始；紧接着的 Break Delimiter 提供上升沿供 Sync 同步。
- **Sync**：从节点没有高精度晶振怎么通信？→ 用 Sync(0x55) 的等距下降沿测出主节点波特率并自校准，因此 LIN 从节点可用廉价 RC 振荡器。
- **PID**：① PID 为什么是"受保护"的？→ 低 6 位是帧 ID，高 2 位是奇偶校验位，接收方校验可检测 ID 传输错误。② ID 范围？→ 0~63，其中 60~61 保留，62~63 用于诊断传输。
- **DATA**：LIN 数据长度怎么定？→ 由调度表条目规定（每帧固定长度），而非帧内自描述。
- **Checksum**：经典和增强校验区别？→ 增强多包含 PID，检错更强；节点必须按约定选同一种，否则校验失败。
- **调度表**：主节点按固定周期触发各从节点，确定性好；帧间隔由主节点插入。

### 3. 典型应用
- 车窗、座椅、雨刮、门锁、氛围灯等无需高带宽的车身节点。

### 4. 常见坑
- 波特率偏差累积 → 长帧尾部采样错；靠 Sync 定期重校准缓解。
- 单线对地短路 → 整个 LIN 网瘫痪。

### 5. 面试易问
- LIN 为什么便宜？→ 单线、从节点无需高精度时钟、主从调度简单。
- 主从 vs CAN 多主？→ LIN 主从确定、带宽低；CAN 多主竞争、实时强。

---

## 三、SPI（串行外设接口）

**定位**：板级高速全双工同步总线，MCU 与外设芯片标配。

### 1. 物理层
- 四线：**SCK**(时钟)、**MOSI**(主出从入)、**MISO**(主入从出)、**CS/SS**(片选，低有效)。
- 主从结构，主节点产生时钟；一主多从靠独立 CS 选通。

### 2. 工作模式（CPOL/CPHA）
- **CPOL**：时钟极性（空闲时高/低）。
- **CPHA**：相位（第1/第2个边沿采样）。
- 4 种组合(0~3)，**主从必须模式一致**否则数据错位。

### 2.5 单次传输的逐位结构

**一次完整 SPI 传输**
```
CS(拉低) → [字节1: 8位在SCK边沿移位] → ... → [字节N] → CS(拉高)
```
- **CS（片选）**：低有效，传输期间保持低；CS 上升沿通常表示本次传输结束、从设备锁存结果。多从设备靠独立 CS 选通。
- **每字节**：8 个 SCK 周期，每个周期主从各移 1 位——**MOSI 主出、MISO 主入，全双工同时发生**（发 1 字节的同时收 1 字节）。
- **位序**：可配 MSB-first 或 LSB-first，主从须一致。
- **采样边沿**：由 CPOL/CPHA 决定（mode0: 上升沿采样、下降沿切换；mode3: 下降沿采样、上升沿切换等）。

**关键控制参数（即"字段"）**

| 参数 | 含义 | 出错后果 |
|------|------|----------|
| CPOL | 时钟空闲电平 | 采样边沿错位→数据错 |
| CPHA | 采样相位 | 同样导致错位 |
| Bit order | MSB/LSB 先 | 字节内位序反 |
| Clock rate | SCK 频率 | 超从设备上限→时序违例 |
| CS polarity | 片选有效电平 | 选不中从设备 |
| CS setup/hold | 片选建立/保持 | 数据未稳定即采样 |

#### SPI 的对应面试题
- **CS**：为什么每段传输要独立 CS？→ CS 界定一次原子传输，拉低选中、拉高锁存；多从设备靠不同 CS 区分。
- **全双工**：SPI 同时收发怎么理解？→ 移位寄存器主从共移，发第 N 字节时同时收第 N 字节（"写即读"），所以很多 SPI 读操作要先 dummy 写以产生时钟。
- **Mode 不匹配**：主从 CPOL/CPHA 不一致会怎样？→ 采样边沿错位，整帧数据错乱；用逻辑分析仪看 SCK/MOSI 对齐手册即可定位。
- **为什么 SPI 没有地址？**→ 靠 CS 选设备，而非地址寻址；一条总线上每从设备一根 CS。
- **时钟太快的问题？**→ 超出从设备最大 SCK 或线长导致建立/保持违例、波形畸变→通信失败。

### 3. 时序要点
- 建立/保持时间：数据在时钟采样沿前后需稳定。
- CS 时序：片选建立/保持、传输间 CS 拉高隔离。
- 速率：可达数十 MHz（受线长/负载/从设备限制）。

### 4. 典型应用（BMS 强相关）
- 配置 CAN 收发器（TJA1145 类）：MCU 经 SPI 写收发器寄存器。
- 外部 ADC / 高压采样前端、RTC、Flash、显示屏。
- 国产替代坑：SPI 波形差异（极性、CS 维持时间、寄存器默认值不同）。

### 5. 调试
- 逻辑分析仪抓 SCK/MOSI/MISO/CS 四线，对比手册时序图。
- 核对 CPOL/CPHA、CS 极性、字节序(MSB/LSB first)。

### 6. 面试易问
- SPI 四线各干什么？→ SCK时钟/MOSI主出/MISO主入/CS选通。
- 通信出错第一查什么？→ CPOL/CPHA 模式、CS 时序、时钟速率、上拉/线长串扰。

---

## 四、I²C / IIC（及 SMBus）

**定位**：板级两线低速总线，多设备地址寻址，常用于传感器/电源管理。

### 1. 物理层
- 两线：**SCL**(时钟)、**SDA**(数据)，**开漏+上拉电阻**（总线与逻辑，谁拉低谁赢）。
- 速率：标准 100k / 快速 400k / 快速+ 1M / 高速 3.4M。

### 2. 逐字段详解（I²C 事务）

**典型写事务（含寄存器指针）**
```
START | ADDR(7)+W(0) | ACK | REG(8) | ACK | DATA(8) | ACK | ... | STOP
```
**典型读事务（含重复起始）**
```
START | ADDR+W | ACK | REG | ACK | RepeatedSTART | ADDR+R(1) | ACK | DATA | NACK | STOP
```

| 字段 | 含义 |
|------|------|
| **START** | SCL 高时 SDA 由高→低，标志事务开始 |
| **ADDR(7/10)+R/W** | 从设备地址（7 位常用，10 位扩展）+ 1 位读写方向 |
| **ACK/NACK** | 每字节后接收方拉低 SDA=ACK；发送方发完最后一字节后接收方回 NACK 表示"结束" |
| **REG** | 寄存器/命令指针（sub-address），指定读写的内部寄存器 |
| **DATA** | 数据字节，可连续多字节（多数器件地址自增） |
| **Repeated START** | 不释放总线再发一次 START，用于"先写寄存器再读"不被其他主设备打断 |
| **STOP** | SCL 高时 SDA 由低→高，释放总线 |

#### 每个字段的对应面试题
- **START/STOP**：为什么靠 SDA 在 SCL 高时的跳变定义？→ 正常数据在 SCL 低时变化，只有 SCL 高时 SDA 跳变才是"起停"特殊事件，避免与数据混淆。
- **ADDR+R/W**：7 位地址空间多大？→ 0~127，0x00(通用呼叫)/0x78~0x7F(10位/保留) 特殊，可用约 112 个。
- **ACK/NACK**：① 为什么要 ACK？→ 确认每字节被对端收到，无 ACK 即知从设备不在/忙。② 读最后一字节为什么发 NACK？→ 通知从设备"我不要再读了"，随后 STOP。
- **Repeated START**：为什么读要先写寄存器地址再重复起始？→ I²C 读需先指定读哪个寄存器，重复起始避免中途释放总线被其他主设备抢走。
- **时钟拉伸**：从设备拉低 SCL 干什么？→ 告诉主设备"我还没准备好"，主设备必须等待，需支持拉伸的主机才能实现。
- **SMBus 扩展**：SMBus 增加命令码(Command)、PEC(包差错校验,CRC-8)、超时(35ms/25ms)；**BMS 电量计**常用 SMBus 上报电压/电流/SOC。

### 3. SMBus（BMS 强相关）
- 基于 I²C 的系统管理总线，定义更严格的电气/超时/命令协议。
- **BMS/电池场景**：智能电池包、电量计(fuel gauge)、充电管理芯片常用 SMBus 上报电压/电流/SOC、接收充放电指令。

### 4. 常见坑
- 上拉电阻阻值不当 → 上升沿过缓（速率上不去）或功耗过大。
- 地址冲突（多同址器件）→ 需硬件引脚改址或每总线单一。
- **总线死锁**：从设备在中途拉低 SDA/SCL 不释放（如复位异常）→ 主设备发若干时钟脉冲或复位恢复。
- 时钟拉伸超时未处理 → 通信挂起。

### 5. 面试易问
- 为什么 I²C 要上拉？→ 开漏结构，靠上拉拉高，实现线与仲裁。
- 总线卡死怎么恢复？→ 主设备手动给 SCL 发 9 个脉冲释放 SDA，或复位从设备。
- SMBus 和 I²C 区别？→ SMBus 更严格（超时、命令协议、电气），常用于电源/电池管理。

---

## 五、UART / USART（异步串行）

**定位**：最通用异步点对点串行，调试与低速外设。

### 1. 物理层
- 两线 **TX/RX**（交叉连接），异步无时钟线，靠双方约定波特率。
- 电平：TTL(3.3/5V)、RS232(±12V)、RS485(差分，长距离多节点)。

### 2. 逐字段详解（UART 帧）

**一帧结构（8N1 为例）**
```
Idle(高) | Start(1位0) | Data(5~9位,LSB先) | Parity(可选1位) | Stop(1/1.5/2位高) | Idle
```

| 字段 | 位宽 | 含义 |
|------|------|------|
| **Idle** | — | 线路空闲为高电平 |
| **Start** | 1 | 显性(0)，标志字符开始，接收方据此对齐波特率相位 |
| **Data** | 5~9 | 数据位，通常 8 位，**LSB 先发** |
| **Parity** | 0/1 | 奇/偶/无校验，检测单 bit 错误 |
| **Stop** | 1/1.5/2 | 高电平，标志字符结束，给接收方恢复时间 |
| **Baud** | — | 每秒符号数，双方必须一致 |

#### 每个字段的对应面试题
- **Start**：为什么需要起始位？→ 异步无时钟线，靠 Start 的下降沿让接收方锁定字符起点、按波特率在中点采样后续位。
- **Data LSB first**：数据为什么 LSB 先发？→ 历史惯例+简化移位寄存器硬件设计。
- **Parity**：奇偶校验能检什么错？→ 仅能检测奇数个 bit 翻转，偶数个错检不出；是弱校验，关键场合用 CRC/重传。
- **Stop 1.5/2 位**：为什么有时用 1.5 或 2 停止位？→ 早期慢设备需要更多恢复时间；也用于帧间间隔，降低连续误判。
- **Baud 误差**：波特率不一致会怎样？→ 每字符累积相位偏移，帧越长末端采样点偏出位中心→乱码；选标准分频使误差<2~3%。
- **流控**：RTS/CTS 干什么？→ 接收方缓冲快满时拉低 CTS 通知发送方暂停，防溢出（硬件流控）。

### 3. 典型应用
- 调试串口(log)、GPS、蓝牙模块、Bootloader 早期交互。

### 4. 常见坑
- 波特率误差累积 → 长帧尾部采样偏移；选支持标准分频的时钟。
- 电平不匹配（TTL 直连 RS232）→ 烧毁 IO。
- 接收用环形缓冲 + DMA，避免高速时丢字节。

### 5. 面试易问
- 为什么异步也能同步采样？→ 起始位对齐，按约定波特率在每比特中点采样。
- 波特率误差过大会怎样？→ 帧越长越偏，尾部采样错位→乱码。

---

## 六、SENT（单边沿调制传感器接口）

**定位**：单线、高精度、低成本传感器接口（SAE J2716）。

### 1. 物理层
- 单线（5V 供电线复用或独立信号线），下降沿携带信息。
- 抗干扰优于模拟电压输出，成本低于 CAN。

### 2. 逐字段详解（SENT 帧，SAE J2716）

**一帧结构**
```
Sync(56 ticks) | Status/Comm(4bit) | Data nibble×1~6 (各4bit) | CRC(4bit) | Pause(可选)
```

| 字段 | 含义 |
|------|------|
| **Sync** | 固定 56 个 tick 宽度的低电平脉冲，接收方据此测出 tick 周期(=1/波特) |
| **Status/Comm** | 4 位状态/通信 nibble，传错误标志、通信位等 |
| **Data nibble** | 每个 4 位数据半字节，**用相邻下降沿的时间间隔编码**（12~27 ticks 表示 0~15） |
| **CRC** | 4 位校验，覆盖 Status+Data，检错 |
| **Pause** | 可选暂停脉冲，标志帧结束、分隔下一帧 |

#### 每个字段的对应面试题
- **Sync**：SENT 怎么同步？→ 固定 56-tick 低脉冲，接收方测其宽度算出本帧的 tick 周期。
- **Data 编码**：为什么用"时间间隔"而不是电压幅值？→ 时间测量抗电源噪声/共模干扰远强于电压比较，适合发动机舱等恶劣电磁环境；精度高且布线简单。
- **nibble 数可变**：一帧能传多少数据？→ 1~6 个数据 nibble（4~24 位），可配置；如 12 位主信号用 3 nibble。
- **CRC**：4 位 CRC 够吗？→ SENT 短帧，4 位 CRC 足够覆盖 nibble 级错误，接收方可下一帧补偿。
- **为什么 SENT 比 CAN 更适合传感器？**→ 单线、极低成本和功耗、强抗扰、定点高精度，缺点是无多主/网络管理能力。

### 3. 典型应用
- 油门踏板、变速箱转速/压力、位置传感器——对精度与成本敏感的模拟量传感器。

### 4. 常见坑
- tick 基准时钟精度影响测量精度；需稳定时钟源。
- 电磁环境干扰下降沿抖动 → 需滤波/屏蔽。

### 5. 面试易问
- SENT 怎么编码数据？→ 用相邻下降沿的时间间隔(tick 数)表示数据，非电压幅值。

---

## 七、FlexRay

**定位**：车规高确定性强实时总线（时间触发为主），用于线控底盘/动力。

### 1. 物理层
- 双通道 A/B，差分，速率可达 **10Mbps**，支持星型/总线型拓扑。
- 通道可冗余（双通道同时发相同数据）。

### 2. 逐字段详解（FlexRay 帧与通信周期）

**一帧结构**
```
帧头(Indicator/Status + Frame ID + Payload Len + Header CRC + Cycle) | 数据段(0~254B) | 帧尾(Frame CRC)
```

**通信周期(Communication Cycle)结构**
```
静态段(Static, TDMA时隙) | 动态段(Dynamic, minislot) | 符号窗口(Symbol) | NIT(网络空闲时间)
```

| 字段/段 | 含义 |
|------|------|
| **Frame ID** | 帧标识符，决定该帧在静态段占用的时隙号(1~2047) |
| **Payload Len** | 数据段长度(0~254 字节) |
| **Header CRC** | 帧头校验，保护 ID/长度等关键字段 |
| **Cycle** | 周期计数(0~63)，用于多周期调度与全局同步 |
| **静态段** | 固定时隙 TDMA，每节点独占时隙，确定性强 |
| **动态段** | 基于 minislot 的 FIFO 仲裁，用于事件型、变长数据 |
| **Symbol Window** | 传媒体符号(如唤醒/冲突)，可省略 |
| **NIT** | 网络空闲时间，用于时钟同步补偿 |

#### FlexRay 的对应面试题
- **Frame ID 与时隙**：静态段怎么保证确定性？→ 每帧绑定固定时隙，节点在指定时隙发，绝不冲突，最坏延迟可计算。
- **静态 vs 动态段**：为什么分两段？→ 静态保关键控制(刹车/转向)确定性；动态段灵活承载诊断/事件数据，提高带宽利用率。
- **全局时基**：多节点怎么对齐时隙？→ 通过 NIT 内的时钟同步算法(冷启动/同步节点)收敛到统一时基。
- **为什么被以太网 TSN 替代趋势？**→ FlexRay 带宽(10M)和成本/复杂度不及千兆车载以太网+TSN，域控架构下 TSN 更灵活。
- **双通道**：A/B 通道冗余干什么？→ 一路故障另一路仍可通信，满足高安全(ASIL D)需求。

### 3. 典型应用
- 高端车线控底盘、动力域高实时通信。

### 4. 现状
- 复杂度高、成本贵；正被**车载以太网(TSN)**逐步替代部分场景。

### 5. 面试易问
- FlexRay 为什么确定性强？→ 静态段 TDMA 固定时隙 + 全局时基同步。
- 和 CAN 比优劣？→ FlexRay 更高带宽/确定性，但复杂昂贵；CAN 简单便宜。

---

## 八、车载以太网（Automotive Ethernet）

**定位**：面向域控制器/摄像头/刷写/OTA 的高带宽网络。

### 1. 物理层
- **100BASE-T1 / 1000BASE-T1**：单对双绞线(UTP)，100/1000 Mbps 全双工。
- 采用**回声消除**实现单对线全双工（收发同缆互不干扰）。
- 对比传统 100BASE-TX（两对线，用于诊断/OBD）。

### 2. 逐字段详解（以太网帧与 TSN）

**IEEE 802.3 以太网帧（车载用）**
```
Preamble(7B) | SFD(1B) | Dst MAC(6) | Src MAC(6) | Type/Len(2) | Payload(46~1500B) | FCS(4) | IFG(12B)
```

**TSN 关键机制（保障实时）**
| 机制 | 标准 | 作用 |
|------|------|------|
| gPTP | 802.1AS | 全网精确时间同步(亚微秒) |
| Qav | 802.1Qav | 基于信用的整形(CBS)，平滑高带宽流 |
| Qbv | 802.1Qbv | 时间感知整形器(TAS)，按门控列表在精确时刻开关队列，给关键流量预留确定性时隙 |

#### 每个字段的对应面试题
- **Preamble/SFD**：前导码和 SFD 干什么？→ 前导码(55...)用于接收方比特同步，SFD(0xD5)标记帧开始。
- **Type/Len**：这个字段怎么区分"类型"还是"长度"？→ ≥1536 解释为 EtherType(如 0x0800=IPv4, 0x22F0=AVB/1722)，≤1500 解释为长度。
- **FCS**：CRC-32 校验覆盖哪些？→ 覆盖 Dst/Src/Type/Payload（不含前导和 FCS 本身），检传输错误。
- **gPTP**：车载为什么需要精确时间同步？→ TSN 的 Qbv 门控依赖统一时基，否则各节点"时间窗"对不齐，确定性失效。
- **Qbv 时间感知整形**：怎么保障关键流量低延迟？→ 用门控列表在精确时刻只开放关键队列，其他流量被挡在窗口外，避免抢占。
- **100BASE-T1 单对线全双工**：靠什么？→ 回声消除(canceller)抵消本端发送对接收的干扰，单对双绞线实现全双工。

### 3. 典型应用
- 域控制器间通信、摄像头/激光雷达原始数据、大批量标定/OTA 刷写。

### 4. 常见坑
- 线缆/连接器需车规(100Ω 阻抗匹配)；非标线导致串扰/丢包。
- 唤醒机制（以太网唤醒帧）与低功耗协同复杂。

### 5. 面试易问
- 100BASE-T1 为什么单对线能全双工？→ 回声消除抵消本端发送干扰。
- TSN 在车载为什么重要？→ 保障关键控制流量的确定性低延迟。

---

## 九、PWM / ICU（定时器接口，非"总线"但属关键外设协议）

### 1. PWM（脉冲宽度调制，输出）
- 定时器比较匹配产生可调**频率/占空比**方波。
- 分辨率由定时器位数与计数频率决定（如 16 位 → 65536 级）。
- **互补 PWM + 死区(deadtime)**：防半桥上下管直通（电机/电源场景）。
- **BMS 应用**：驱动主动均衡电路、风扇调速、保险丝精确引爆（控制脉冲能量）。

### 1.5 PWM 参数逐参数详解

**一个 PWM 周期的参数**
```
周期 T = 1/f  ──┐        ┌──── 占空比 D = Ton / T
   ┌──Ton──┐   │        │
   │      │   │        │
___│      │___│________│___  (Ton 高电平, Toff 低电平)
```

| 参数 | 含义 | 关系 |
|------|------|------|
| **f (频率)** | 1/T，每秒周期数 | 由定时器时钟分频 + 自动重装载值(ARR)决定 |
| **ARR** | 计数周期值，定 T | T = (ARR+1) / f_tim |
| **CCR** | 比较值，定 Ton | Ton = (CCR+1)/f_tim，占空比=CCR/ARR |
| **分辨率** | 可调精细度 | = log2(ARR+1)，如 ARR=65535 → 16 位分辨率 |
| **Deadtime** | 互补通道间都关的间隙 | 防上下管同时导通短路 |
| **极性** | 高有效/低有效 | 决定 Ton 在高低电平哪段 |

#### PWM 的对应面试题
- **占空比怎么算**？→ Ton/T，由比较寄存器 CCR 与重装载 ARR 比值决定。
- **分辨率不够**？→ 提高定时器位数或降低频率(增大 ARR)以提升占空比级数。
- **死区为什么必须**？→ 半桥上下管若同时导通→电源直通短路；死区强制两管都关一小段。
- **BMS 保险丝引爆**？→ 用精确脉宽控制通过火工品的能量，过短不引爆、过长危险。

### 2. ICU（输入捕获，输入）
- 捕获外部信号边沿，配合定时器计数**精确测频率/占空比/脉宽**。
- **BMS 应用**：碰撞信号检测（高频脉冲判定）、转速测量。
- 对比纯中断计数：ICU 硬件计时精度高、不占 CPU。

### 2.5 ICU 测量参数详解

**用 ICU 测一个周期信号**
```
上升沿捕获 t1 ──── 下降沿捕获 t2 ──── 下一上升沿捕获 t3
周期 T = t3 - t1 ；高电平脉宽 = t2 - t1 ；占空比 = (t2-t1)/T
```

| 参数 | 测得方法 |
|------|----------|
| **周期 T** | 两相邻同边沿(上升-上升)捕获的时间差 |
| **脉宽** | 相邻上升/下降沿时间差 |
| **占空比** | 脉宽 / 周期 |
| **频率** | 1/T |

#### ICU 的对应面试题
- **ICU 比中断计数好在哪**？→ 硬件在边沿瞬间锁存定时器值，不受中断延迟/抖动影响，精度高且不占 CPU。
- **怎么测高频信号**？→ 高频时直接数定时器溢出+边沿次数，或用 ICU 测单周期再取倒数；超 ICU 上限需分频。
- **碰撞信号怎么用 ICU**？→ 碰撞产生特定脉冲，ICU 捕获脉宽/频率判定是否真实碰撞，触发断高压。

### 3. 常见坑
- 死区时间不足/过长 → 直通风险或输出畸变。
- 占空比分辨率不足 → 控制精度差。

### 4. 面试易问
- PWM 和 ICU 分别干什么？→ PWM 输出可控方波(驱动)，ICU 输入测量频率/占空比。
- 为什么半桥要死区？→ 防止上下管同时导通短路。

---

## 十、ADC（模拟-数字转换接口）

**定位**：把电压/温度等模拟量转数字，底层软件的"感官"。

### 1. 类型与参数
- **SAR**（逐次逼近，中速中精度，常用）vs **Σ-Δ**（高精度低速，称重/音频）。
- 关键参数：分辨率(10/12/16bit)、采样率、输入范围、参考电压(Vref)。

### 2. 触发与搬运 + 逐时序详解
- **硬件触发 + DMA**：定时器/PWM 触发采样，DMA 自动搬结果，零 CPU 干预、低抖动。
- 多通道：扫描模式 + 多路 MUX 切换。

### 2.5 单次采样时序逐字段详解

**一次 ADC 转换的时间构成**
```
[采样保持 S/H] → [转换 Conversion] → [结果锁存]
   ↑建立时间        ↑位数决定
```
- **采样阶段(S/H)**：开关闭合，外部信号给内部采样电容充电，需 ≥ 建立时间(Settling) 才准确。
- **转换阶段**：SAR 逐位比较(或 Σ-Δ 调制)，位数越多转换越久。
- **触发源**：软件触发 / 定时器触发 / 外部事件触发（硬件触发免抖动）。

| 参数 | 含义 |
|------|------|
| **分辨率** | 位数(10/12/16)，决定最小量化步长 = Vref/(2^N) |
| **采样率** | 每秒可完成转换次数 |
| **Vref** | 参考电压，决定满量程与精度基准 |
| **建立时间** | 采样电容充电达标所需时间，< 它则精度降 |

#### ADC 的对应面试题
- **建立时间**：采样周期太短会怎样？→ 电容未充满→量化值偏低/不准，系统性误差。
- **Vref 不稳**：对精度影响？→ 步长与满量程随 Vref 漂移，绝对精度下降。
- **分辨率 vs 精度**：12 位就一定准吗？→ 不一定，精度还受 Vref、INL/DNL、噪声、建立时间影响；分辨率只是"能分多少级"。
- **硬件触发+DMA 价值**？→ 定时精确、零 CPU、无中断抖动，适合周期性高压采样。

### 3. 精度保障（BMS 高压采样关键）
- 满足**建立时间(Settling Time)**：采样开关闭合后电容需充满，采样周期太短→未充满→精度下降。
- 参考电压稳定、内部 offset/gain 校准、通道顺序避开串扰。
- 硬件滤波（RC/运放跟随）+ 软件滤波（平均/中值）。

### 4. BMS 应用
- 高压采样：分压网络 + 多路 MUX + ADC 采单体/总压（mV 级精度）。
- 温度：NTC 电阻→电压→ADC→温度换算。

### 5. 常见坑
- 采样周期 < 建立时间 → 精度系统性偏低。
- 输入阻抗不匹配 → 采样误差。
- MUX 切换后未留建立时间 → 串入前一路电压。

### 6. 面试易问
- 采样精度怎么保障？→ 建立时间、Vref 稳、校准、通道顺序、滤波。
- 采样周期太短会怎样？→ 电容未充满→精度下降、串扰。

---

## 十一、GPIO（通用输入输出）

**定位**：最基础的数字 IO，连接状态与控制。

### 1. 工作模式（及寄存器级逐字段）
- 输入 / 输出 / 复用(外设功能) / 模拟(ADC 输入)。
- 输出：推挽(强高强低) vs 开漏(仅拉低，需上拉)。
- 上下拉电阻、斜率控制(降低 EMI)。

### 1.5 GPIO 寄存器逐字段（以通用 IP 为例）

| 寄存器 | 字段 | 含义 |
|--------|------|------|
| **DIR** | 每 bit 1/0 | 方向：1=输出，0=输入 |
| **OUT** | 每 bit | 输出值 |
| **IN** | 每 bit | 输入采样值(只读) |
| **PU/PD** | 每 bit | 上拉/下拉使能 |
| **MUX/AF** | 每 pin 2~4bit | 复用功能选择(接 UART/SPI/定时器…) |
| **INT_EN** | 每 bit | 中断使能 |
| **INT_TYPE/POL** | 每 bit | 边沿(上升/下降/双)或电平触发 |
| **INT_STAT** | 每 bit | 中断挂起标志(写1清) |

#### GPIO 的对应面试题
- **推挽 vs 开漏**？→ 推挽主动驱动高低；开漏只拉低、需外部上拉，用于线与(I²C)或电平转换。
- **输入模式悬空**？→ 浮空引脚易受干扰误触发/耗电，未用脚应配上下拉或模拟输入。
- **复用(MUX)**？→ 一个物理 pin 可映射多个外设，靠 AF 寄存器选择；配置错则外设收不到信号。
- **中断边沿 vs 电平**？→ 边沿适合事件计数；电平适合持续状态，但需及时清标志防反复触发。

### 2. 中断
- 边沿触发(上升/下降/双沿) 或 电平触发。
- 用于按键、唤醒源、外部事件通知。

### 3. BMS/汽车应用
- 唤醒源（钥匙/充电枪插入）、状态指示 LED、片选(CS) 控制、高压互锁(HVIL) 检测。

### 4. 常见坑
- 未用引脚悬空 → 耗电/误触发，应配上下拉或模拟输入。
- 输出驱动能力不足 → 加驱动/缓冲。

### 5. 面试易问
- 推挽和开漏区别？→ 推挽能主动输出高低，开漏仅拉低需上拉（用于线与/电平转换）。
- 休眠电流高怎么查 GPIO？→ 查未用引脚是否悬空/误配，IO 是否处高功耗态。

---

## 十二、DSI3（BMS 电芯监控专用菊花链，重点）

**定位**：电池管理系统(BMS)中，MCU 与**电芯监控芯片（Cell Monitoring IC）**之间的专用通信协议。

### 1. 背景
- 动力电池包有数十~上百节电芯，需在高压环境里采集每节电压/温度。
- 电芯监控 IC（如 ADI LTC68xx 系列、NXP MC3377x 系列）负责就近采集，再通过**隔离菊花链**上报给主控 MCU。
- 对比 **isoSPI**（ADI 的隔离 SPI 变体，用变压器/电容隔离的差分双线）：DSI3 是另一主流菊花链协议标准。

### 2. 物理层
- **差分/隔离**：通过变压器或电容耦合实现高低压隔离（电芯侧高压 vs 控制侧低压）。
- 菊花链(daisy-chain)：多级监控 IC 串联，首尾接 MCU，命令/数据逐級转发。
- 高抗共模干扰、抗电磁噪声（电池包内强电流/强磁场环境）。

### 3. 协议要点（逐字段）
- 命令/响应机制：MCU 下发读取电压/温度/均衡控制命令，各监控 IC 应答。
- 帧含同步、地址/命令、数据、CRC 校验。
- 支持**均衡控制命令**（主动/被动均衡启停）、故障状态上报（过压/欠压/温度）。

### 3.5 帧字段详解（典型菊花链命令帧）

**一帧结构（概念）**
```
Sync(同步头) | ADDR(芯片地址) | CMD(命令/寄存器) | DATA(载荷) | CRC | (环回/响应)
```

| 字段 | 含义 |
|------|------|
| **Sync** | 帧同步头，界定一帧开始（隔离变压器耦合下的编码同步） |
| **ADDR** | 目标监控 IC 地址；菊花链中命令会逐级转发/过滤 |
| **CMD** | 操作码：读电压/温度、启动均衡、读故障状态等 |
| **DATA** | 读出值(电压/温度)或写入的均衡参数 |
| **CRC** | 校验，强电磁环境必备，防误控电芯 |
| **响应/环回** | 末端 IC 响应沿链回传，或环形(ring)拓扑双向 |

#### DSI3 的对应面试题
- **菊花链怎么工作**？→ 命令从 MCU 发出，逐级监控 IC 转发，末级应答再逐级回传；每级有地址，只响应自己的命令。
- **为什么用隔离(变压器/电容)**？→ 电芯侧是高压(数百V)、控制侧是低压，必须隔离保护 MCU 并抗共模干扰。
- **CRC 为什么关键**？→ 电池包强电流/磁场噪声大，一位错可能误判过压→误断高压，CRC 杜绝错误控制。
- **某级故障怎么办**？→ 用环形(ring)拓扑或故障旁路设计，单级失效不至于整链瘫痪。
- **DSI3 vs isoSPI**？→ 都是隔离菊花链，不同厂商标准(ADI isoSPI 用差分 SPI over 变压器，NXP 等用 DSI3)，本质解决同样问题。

### 4. BMS 应用价值
- 实现单体电压 mV 级采样、温度多点监测、均衡控制——是 BMS "看得见每节电芯" 的基础。
- 隔离通信保障低压侧 MCU 不被高压损坏。

### 5. 常见坑
- 变压器/电容隔离元件选型与layout 不当 → 通信误码。
- 菊花链某级故障 → 整链中断，需环回(ring)拓扑或故障隔离设计。
- 位时序/时钟偏差在长链累积 → 末级采样不稳。
- 强电流噪声耦合 → 需屏蔽与滤波。

### 6. 面试易问
- BMS 怎么采集上百节电芯电压？→ 电芯监控IC就近采集 + 隔离菊花链(DSI3/isoSPI) 上报主控。
- 为什么要用隔离通信？→ 高低压隔离，保护低压 MCU、保障安全。
- DSI3 和 isoSPI 区别？→ 都是隔离菊花链，不同厂商标准(协议/物理层细节不同)，本质解决同样问题。

---

## 十三、PSI5 / SPC（传感器接口）

**定位**：两线供电+通信的传感器总线，用于安全相关传感器。

### 1. PSI5（Peripheral Sensor Interface 5）
- 两线：同时给传感器供电 + 传输数据（电流/电压调制）。
- 主从、同步，低功耗、高可靠。
- 应用：气囊加速度传感器、压力传感器等安全件。

### 1.5 PSI5 位/帧详解
**一帧（同步曼彻斯特编码）**
```
Sync(同步) | Start | Data bit×N(曼彻斯特编码) | CRC | Idle
```
- 数据在供电线用**电流或电压跳变(曼彻斯特)**编码，主设备提供同步脉冲。
- 两线既供 5~15mA 工作电流，又承载数据，省线省连接器。

| 字段 | 含义 |
|------|------|
| **Sync** | 主设备发同步脉冲，启动一次传输 |
| **Data** | 传感器数据，曼彻斯特编码(0/1 由跳变方向定) |
| **CRC** | 校验，安全件必备 |

### 2. SPC（SENT with PSI5-compatible）
- 时钟同步的 PSI5 变体，兼容 SENT 编码、由主设备提供同步时钟，提升精度。

#### PSI5/SPC 的对应面试题
- **两线怎么同时供电通信**？→ 在供电电流/电压上叠加曼彻斯特编码的数据跳变，接收端从线上解调。
- **为什么适合安全传感器(气囊)**？→ 两线简单可靠、抗扰、可单点诊断，满足高安全(ASIL)要求。
- **PSI5 与 SPC 区别**？→ SPC 由主设备提供同步时钟(类似 SENT 的同步)，时序更准、可在同一总线下混用 SENT/PSI5 设备。
- **曼彻斯特编码作用**？→ 自带时钟信息(每位必有跳变)，接收方易同步且直流平衡。

### 3. 面试易问
- PSI5 两线怎么同时供电和通信？→ 在供电线上叠加调制信号传数据。
- 和 SENT 区别？→ PSI5 两线供电通信一体、多用于安全传感器；SENT 单线纯信号。

---

## 十四、DMA（直接内存访问，总线主设备搬运引擎）

**定位**：DMA 控制器（DMAC）是一个能主动发起总线事务的**总线主设备**，把数据在"外设寄存器 ↔ 内存"或"内存 ↔ 内存"之间搬运，全程无需 CPU 执行取/存指令，用来解放 CPU、提供确定性时序、降低功耗。它不是"协议"，而是所有高速外设背后的搬运基础设施（ADC 连续采样、UART 高速收发、SPI 屏、音频、MAC 收发包都依赖它）。详细章节见 [articles/p14-dma.md](articles/p14-dma.md)。

### 1. 概念与角色
- 与 CPU 并列挂在总线矩阵/互联上，经仲裁共享总线；突发过长/优先级过高会 stall CPU 取指（"带宽博弈"）。
- 类比：外设是产线工位、CPU 是总经理、内存是档案室，DMA 是"物流组"——有通行证自行存取，事后发中断通知。

### 2. 传输参数逐参数详解
- **方向（Direction）**：P2M（外设→内存，如 ADC 采样）、M2P（内存→外设，如 UART TX）、M2M（内存→内存，非所有 DMAC 支持）。
- **数据宽度（Width）**：8/16/32 位；外设侧与内存侧可不同（靠内部 FIFO 宽度转换打包）。未对齐访问（如 32 位非 4 字节对齐）会总线错误。
- **地址增量（Increment）**：外设地址通常固定、内存地址通常递增；写反会导致样本叠同一位置。
- **突发长度（Burst）**：一次总线事务连续搬 1/4/8/16 beat，摊薄地址相位、提升效率，但过长会 stall 其他主设备。
- **传输计数（Count）**：本次搬的数据项数（CNDTR 是真实项数，非"长度-1"）；循环模式下减到 0 自动重装。
- **优先级 + 仲裁**：通道优先级 + 同优先级轮询；DMAMUX 把 N 个外设请求灵活路由到 M 个通道。
- **流控（Flow Control）**：外设流控（由 DRQ 个数驱动，如 UART RX）vs 内存流控（按计数搬完，如 M2M）。

### 3. 工作模式与机制
- **循环模式（Circular）**：计数到 0 自动重装，持续流（ADC 采样/音频）。
- **双缓冲/乒乓（Double Buffer）**：两块内存交替，HT/TC 中断通知 CPU 处理"当前未被 DMA 写"的那块，免锁。
- **半/全传输中断（HT/TC）**：HT 时前半区稳定、TC 时后半区稳定；只处理 DMA 没在写的半区。
- **散聚（Scatter-Gather）**：内存里描述符链表，DMAC 自主遍历多块不连续缓冲（eDMA/PL330 风格），描述符本身也需缓存维护。
- **错误标志**：TC（完成）/HT（半）/TE（传输错误，多为地址/宽度错或非法地址）/FE（FIFO 错）。

### 4. 与典型外设协作
- **ADC 连续采样（P2M+循环+HT/TC）**：规则组只有一个共享 DR，多通道扫描必须用 DMA 及时搬走，否则被覆盖并置 OVR。
- **UART RX（P2M）+ IDLE 中断**：DMA 持续收，IDLE 表示一帧结束，读 CNDTR 算已收长度；比每字节进中断省海量 CPU。
- **SPI 全双工**：收发两通道并行（M2P 发 + P2M 收），计数须一致。
- **与定时器联动**：TIM TRGO → 外设触发 → DRQ → DMA 搬，采样时刻由硬件决定，确定性采样标准范式。

### 5. 常见坑
- 缓存不一致（Cortex-M7 写回缓存）：TX 前 Clean、RX 后 Invalidate，按缓存行对齐。
- 宽度/地址未对齐 → 总线错误 TE；外设/内存增量写反 → 样本叠一处。
- 循环缓冲半/全中断处理不当 → 读到新旧混合；溢出/欠载（外设与 DMA 速率不匹配）。
- 优先级饥饿（DMA 占满总线 stall CPU）；请求握手（边沿/电平）配错丢请求；缓冲放栈上被踩。
- 低功耗未停 DMA/未等 TC → 数据截断；M2M 不支持或流控错配盲目搬。

### 6. 面试易问
- DMA 为什么能解放 CPU？→ 总线主设备，CPU 负载从 O(N) 降到 O(1)。
- 循环 vs 双缓冲？→ 循环连续流；双缓冲两块交替、免锁处理流式数据。
- HT/TC 为什么免锁？→ 只处理 DMA 当前没在写的半区。
- 缓存一致性怎么解决？→ 写回缓存下 TX Clean / RX Invalidate，按缓存行对齐。
- 为什么规则组多通道 ADC 必须用 DMA？→ 共享 DR 被覆盖 + OVR。
- DMA 太贪婪会怎样？→ stall CPU 取指，需限制突发/优先级+轮询/TCM。
- UART 不定长帧怎么收？→ DMA + IDLE 中断读 CNDTR 算长度。

---

## 速记清单（外设协议对比）

| 协议 | 线数 | 速率 | 拓扑 | 典型场景 |
|------|------|------|------|----------|
| CAN/CAN FD | 2差分 | ≤1M / 数据段8M | 多主总线 | 整车/动力/BMS通信 |
| LIN | 1 | ≤20k | 主从 | 车身低成本 |
| SPI | 4 | 数十M | 主从(多CS) | 外设配置/ADC/收发器 |
| I²C/SMBus | 2 | ≤3.4M | 多主从(地址) | 传感器/电源/BMS电量计 |
| UART | 2 | 低 | 点对点 | 调试/外设 |
| SENT | 1 | 低 | 点对点 | 高精度传感器 |
| FlexRay | 2×差分 | 10M | 总线/星型 | 线控底盘 |
| 车载以太网 | 1对双绞 | 100/1000M | 交换 | 域控/摄像头/OTA |
| PWM | 1 | — | 输出 | 均衡/风扇/引爆 |
| ICU | 1 | — | 输入 | 测频/碰撞 |
| ADC | 模拟 | — | 输入 | 高压采样/温度 |
| GPIO | 1 | — | 输入输出 | 唤醒/状态/片选 |
| DSI3 | 差分隔离 | 中 | 菊花链 | BMS电芯监控 |
| PSI5/SPC | 2(供电+通信) | 低 | 主从 | 安全传感器 |

---

## 面试高频自测

1. CAN 非破坏性仲裁怎么理解？→ 逐位比较，输方自动退出发送，高优先级无延迟。
2. 为什么 CAN 要 120Ω 终端？→ 阻抗匹配消除反射。
3. SPI 四模式由什么决定？→ CPOL(时钟极性)+CPHA(采样相位)。
4. I²C 总线死锁如何恢复？→ 主发 9 个时钟脉冲释放 SDA，或复位从设备。
5. SMBus 与 BMS 什么关系？→ 智能电池/电量计常用 SMBus 上报电压电流SOC。
6. BMS 怎么采上百节电芯电压？→ 电芯监控IC+隔离菊花链(DSI3/isoSPI)。
7. PWM 死区为什么需要？→ 防半桥上下管直通短路。
8. ADC 精度靠什么保障？→ 建立时间、Vref、校准、通道顺序、滤波。
9. CAN FD 数据段为何能提速？→ 仲裁后双方已同步，数据段切高速率。
10. 车载以太网单对线全双工靠什么？→ 回声消除。
